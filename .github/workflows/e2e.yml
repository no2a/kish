name: end to end test

on:
  - push
  - pull_request

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v3.1.0
      with:
        go-version: 1.17

    - name: Test
      run: ./e2e.bash
